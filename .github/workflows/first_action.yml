
name: first workflow

on: workflow_dispatch
jobs:
  first-job:
    runs on: ubuntu_latest
    steps:
      - name: Print greeting
        run: echo "Hello World"
      - name: Print Goodbye
        run: echo "Goodbye"
